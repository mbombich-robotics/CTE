# Shelly plug MAC address(es).
# Find the MAC in the Windows Mobile Hotspot connected-devices list,
# or in the Shelly web UI → Settings → Wi-Fi status.
# The scheduler resolves the current IP at startup via ARP — no static IP needed.
SHELLY_MAC_1=ac:eb:e6:f4:c3:ac
# SHELLY_MAC_2=ac:eb:e6:xx:xx:xx
